language: python
python:
  - 2.6
  - 2.7
  - 3.2
  - 3.3
  - 3.4
install:
  - pip install -r extra/requirements-test.txt
  - pip install .
script:
  - py.test tests
branches:
  except:
    - /^play\/.*$/
